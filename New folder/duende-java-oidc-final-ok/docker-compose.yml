services:
  duende-idsrv:
    build:
      context: ./duende-idsrv
    ports:
      - "5000:80"

  springboot-app:
    build:
      context: ./springboot-app
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=default